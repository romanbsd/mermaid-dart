name: mermaid_dart_flutter_example
description: Gallery demo for every Mermaid renderer supported by mermaid_dart.
publish_to: none

environment:
  sdk: ^3.12.2
  flutter: ">=3.44.0"

dependencies:
  cupertino_icons: ^1.0.8
  flutter:
    sdk: flutter
  mermaid_dart:
    path: ../../..
  mermaid_dart_flutter:
    path: ..

dev_dependencies:
  flutter_test:
    sdk: flutter
  lints: ^6.0.0

flutter:
  uses-material-design: true
